<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Vinyls" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="colorSurface">@color/card_dark</item>
        <item name="android:colorBackground">@color/bg_dark</item>
        <item name="colorPrimary">@color/accent_purple</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="Theme.Vinyls" parent="Base.Theme.Vinyls" />

    <style name="MyDatePickerDialog" parent="Theme.MaterialComponents.Light.Dialog">

        <!-- Your existing styles -->
        <item name="colorBackgroundFloating">@color/white</item>

        <!-- Force OK/Cancel text color (API 21–34) -->
        <item name="android:textColorAlertDialogListItem">@color/black</item>

        <!-- Holo / AppCompat fallback (older devices, emulators) -->
        <item name="android:buttonStyle">@style/MyDialogButtonStyle</item>

        <!-- MaterialComponents fallback (newer devices) -->
        <item name="materialButtonStyle">@style/MyDialogButtonStyle</item>

    </style>

    <style name="MyDialogButtonStyle" parent="@android:style/Widget.Material.Button.Borderless">
        <item name="android:textColor">@color/black</item>
    </style>


</resources>